/**
 * Role-based service layer packages.
 *
 * <ul>
 *   <li>{@code admin} — 관리자 사용자·증거·로그·대시보드</li>
 *   <li>{@code analysis} — 분석 요청·워커·결과·영상 모듈</li>
 *   <li>{@code auth} — 로그인·가입·초대코드·리프레시 토큰</li>
 *   <li>{@code blockchain} — 블록체인 앵커</li>
 *   <li>{@code compare} — 비교 검증</li>
 *   <li>{@code custody} — CoC 감사 로그·체인 검증·Recovery Score</li>
 *   <li>{@code dashboard} — 대시보드 통계·캐시</li>
 *   <li>{@code evidence} — 증거 업로드·상세·사본·해시</li>
 *   <li>{@code integrity} — 무결성 검증·보안 알림 (SEC-153)</li>
 *   <li>{@code manifest} — Evidence Manifest·X.509 서명</li>
 *   <li>{@code notification} — 인앱 알림</li>
 *   <li>{@code report} — PDF 리포트</li>
 *   <li>{@code user} — 프로필·마이페이지·설정</li>
 * </ul>
 */
package com.example.demo.service;
